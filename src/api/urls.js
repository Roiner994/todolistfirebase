const urlBase = "http://localhost:8000/api/";

export const urlServer = "http://localhost:8000/";

export const urlUsers = `${urlBase}users`;

export const urlAuth = `${urlBase}users/login`;

export const urlCategories = `${urlBase}categories`;

export const urlProducts = `${urlBase}products`;

export const urlPathProducts = `${urlBase}path/images/products`;